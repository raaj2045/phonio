# Phonio - Add/Delete Contacts App

## Setting up locally

1. Clone the repo and cd into Phonio/phone-directory.
2. Run ```npm install``` to install all dependencies.
3. Run ```npm start``` to start server at localhost:3000.